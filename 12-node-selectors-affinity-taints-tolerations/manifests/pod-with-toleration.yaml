apiVersion: v1
kind: Pod
metadata:
  name: pod-with-toleration
  namespace: lab-12-scheduling
spec:
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "special"
    effect: "NoSchedule"
  containers:
  - name: nginx
    image: nginx:stable
