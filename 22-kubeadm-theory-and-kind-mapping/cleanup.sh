#!/usr/bin/env bash
set -euo pipefail

echo "Lab 22 is read-only — no cluster objects were created."
echo "The etcd static pod deleted during the lab was automatically recreated by kubelet."
echo "Nothing to clean up."
