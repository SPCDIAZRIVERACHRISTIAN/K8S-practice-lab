apiVersion: v1
kind: Service
metadata:
  name: nginx-broken
  namespace: lab-06-services
spec:
  type: ClusterIP
  selector:
    app: nginnx    # typo: "nginnx" instead of "nginx" — no pods match this selector
  ports:
  - port: 80
    targetPort: 80
